Placeholder — pending real logo file from client.

Expected: `Logo-jafra.png` in this directory.
Meanwhile a temporary `Logo-jafra.svg` is used so the UI doesn't break.
When the real PNG arrives, place it here and update the reference in
`data/customerDemos.ts` (entry `slug: "jafra"` → `logo`) to `Logo-jafra.png`.
